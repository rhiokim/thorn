{
  "name": "thorn-zmq",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint 'lib/**/*.js'",
    "start": "node .",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 1",
    "docker:images": "docker images | grep thorn-zmq",
    "docker:rm": "docker rm -f thorn-zmq",
    "docker:run": "docker run -dit -p 8082:8082 -e ZMQ_MODE=pub -e ZMQ_HOST=127.0.0.1 --name thorn-zmq thorn-zmq:latest",
    "docker:build": "docker build --tag thorn-zmq . && npm run docker:images"
  },
  "dependencies": {
    "zeromq": "^4.1.1"
  }
}
